## JavaScript Objects

## **all commonly used JavaScript Number methods** — both **instance methods** (used on number values) and **static methods** (used on `Number.` class) — with **short examples**.

---

## ⚙️ Instance Methods

(Use them on number values like `let n = 123.45;`)

---

### 🧮 `toExponential()`

Shows number in **exponential (scientific)** form.

```javascript
let n = 1234.56;
console.log(n.toExponential()); // "1.23456e+3"
```

---

### 💰 `toFixed(digits)`

Formats number with **fixed decimals**.

```javascript
let n = 5.6789;
console.log(n.toFixed(2)); // "5.68"
```

---

### 📍 `toPrecision(digits)`

Shows number with **total number of digits**.

```javascript
let n = 123.456;
console.log(n.toPrecision(4)); // "123.5"
```

---

### 🌍 `toLocaleString()`

Formats number according to **local settings**.

```javascript
let n = 1234567.89;
console.log(n.toLocaleString()); // e.g. "1,234,567.89"

console.log(num.toLocaleString("en-US")); // US format
console.log(num.toLocaleString("en-IN")); // Indian format

```

---

### 🔡 `toString()`

Converts number to **string**.

```javascript
let n = 255;
console.log(n.toString());    // "255"
console.log(n.toString(16));  // "ff" (hex)
```

---

### 🧠 `valueOf()`

Returns the **primitive number value** of a Number object.

```javascript
let n = new Number(10);
console.log(n.valueOf()); // 10
```

---

## ⚙️ Static Methods

(Use them on `Number.` class like `Number.isNaN(...)`)

---

### ⚠️ `Number.isNaN(x)`

Checks if value is **NaN** (not a number).

```javascript
console.log(Number.isNaN("abc"));    // false
console.log(Number.isNaN(NaN));      // true
```

---

### ♾️ `Number.isFinite(x)`

Checks if number is **finite**.

```javascript
console.log(Number.isFinite(100));   // true
console.log(Number.isFinite(Infinity)); // false
```

---

### 🔢 `Number.isInteger(x)`

Checks if number is an **integer**.

```javascript
console.log(Number.isInteger(10));     // true
console.log(Number.isInteger(10.5));   // false
```

---

### 🛡️ `Number.isSafeInteger(x)`

Checks if number is a **safe integer**.

```javascript
console.log(Number.isSafeInteger(10));            // true
console.log(Number.isSafeInteger(9007199254740992)); // false
```

---

### 📥 `Number.parseInt(str)`

Converts **string → integer**.

```javascript
console.log(Number.parseInt("25px")); // 25
```

---

### 📥 `Number.parseFloat(str)`

Converts **string → float**.

```javascript
console.log(Number.parseFloat("25.7px")); // 25.7
```

---

## ✅ Quick Summary Table

| Instance Method    | Use                    |
| ------------------ | ---------------------- |
| `toExponential()`  | Exponential format     |
| `toFixed()`        | Fixed decimal places   |
| `toPrecision()`    | Total number of digits |
| `toLocaleString()` | Local formatting       |
| `toString()`       | To string              |
| `valueOf()`        | Primitive value        |

| Static Method            | Use              |
| ------------------------ | ---------------- |
| `Number.isNaN()`         | Is NaN           |
| `Number.isFinite()`      | Is finite        |
| `Number.isInteger()`     | Is integer       |
| `Number.isSafeInteger()` | Is safe integer  |
| `Number.parseInt()`      | String → integer |
| `Number.parseFloat()`    | String → float   |

---


# <><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><>

Perfect! Here’s a **super short, one-line cheat sheet** for all **34 String instance methods** — easy to memorize for interviews:

---

**1. `at(index)`** → char at index. `"Hello".at(1)` → `"e"`

**2. `charAt(index)`** → char at index. `"Hello".charAt(1)` → `"e"`

**3. `charCodeAt(index)`** → Unicode of char. `"Hello".charCodeAt(1)` → `101`

**4. `codePointAt(index)`** → Unicode code point. `"💖".codePointAt(0)` → `128150`

**5. `concat(str)`** → combine strings. `"Hello".concat(" World")` → `"Hello World"`

**6. `endsWith(substr)`** → check end. `"Hello".endsWith("lo")` → `true`

**7. `includes(substr)`** → check existence. `"Hello".includes("el")` → `true`

**8. `indexOf(substr)`** → first occurrence index. `"Hello".indexOf("l")` → `2`

**9. `lastIndexOf(substr)`** → last occurrence index. `"Hello".lastIndexOf("l")` → `3`

**10. `localeCompare(str)`** → compare strings. `"a".localeCompare("b")` → `-1`

**11. `match(regex)`** → regex match. `"Hello".match(/l/g)` → `["l","l"]`

**12. `matchAll(regex)`** → all regex matches. `[..."Hello".matchAll(/l/g)]` → `[["l"],["l"]]`

**13. `normalize()`** → Unicode normalization. `"é".normalize()` → `"é"`

**14. `padEnd(len, str)`** → pad end. `"Hi".padEnd(5,".")` → `"Hi..."`

**15. `padStart(len, str)`** → pad start. `"Hi".padStart(5,".")` → `"...Hi"`

**16. `raw()`** → raw template string. `String.raw`Hello\nWorld\`\` → `"Hello\\nWorld"`

**17. `repeat(n)`** → repeat string n times. `"Hi".repeat(3)` → `"HiHiHi"`

**18. `replace(search, replace)`** → replace first. `"Hello".replace("l","x")` → `"Hexlo"`

**19. `replaceAll(search, replace)`** → replace all. `"Hello".replaceAll("l","x")` → `"Hexxo"`

**20. `search(regex)`** → first match index. `"Hello".search(/l/)` → `2`

**21. `slice(start,end)`** → substring. `"Hello".slice(1,4)` → `"ell"`

**22. `split(sep)`** → split to array. `"Hello".split("")` → `["H","e","l","l","o"]`

**23. `substr(start,len)`** → substring with length. `"Hello".substr(1,3)` → `"ell"`

**24. `substring(start,end)`** → substring between indexes. `"Hello".substring(1,4)` → `"ell"`

**25. `toLocaleLowerCase()`** → lowercase (locale). `"HELLO".toLocaleLowerCase()` → `"hello"`

**26. `toLocaleUpperCase()`** → uppercase (locale). `"hello".toLocaleUpperCase()` → `"HELLO"`

**27. `toLowerCase()`** → lowercase. `"HELLO".toLowerCase()` → `"hello"`

**28. `toString()`** → string representation. `(123).toString()` → `"123"`

**29. `toUpperCase()`** → uppercase. `"hello".toUpperCase()` → `"HELLO"`

**30. `toWellFormed()`** → well-formed string. `"💖".toWellFormed()` → `"💖"`

**31. `trim()`** → remove spaces both ends. `"  hello  ".trim()` → `"hello"`

**32. `trimEnd()` / `trimRight()`** → remove spaces end. `"  hello  ".trimEnd()` → `"  hello"`

**33. `trimStart()` / `trimLeft()`** → remove spaces start. `"  hello  ".trimStart()` → `"hello  "`

**34. `valueOf()`** → primitive string. `"Hello".valueOf()` → `"Hello"`

---

✅ **Tip for memorizing:**

* **Character & index:** `at, charAt, charCodeAt, codePointAt`
* **Modify / combine:** `concat, replace, replaceAll, repeat, padStart, padEnd`
* **Search / find:** `includes, indexOf, lastIndexOf, search, match, matchAll, localeCompare`
* **Case:** `toLowerCase, toUpperCase, toLocaleLowerCase, toLocaleUpperCase`
* **Trim / clean:** `trim, trimStart, trimEnd`
* **Primitive / string conversion:** `toString, valueOf, raw, toWellFormed`

---

String Methods
│
├── Character & Index
│   ├─ at(index)            → char at index
│   ├─ charAt(index)        → char at index
│   ├─ charCodeAt(index)    → Unicode number
│   └─ codePointAt(index)   → Unicode code point
│
├── Modify / Combine
│   ├─ concat(str)          → combine strings
│   ├─ replace(search, rep) → replace first match
│   ├─ replaceAll(search, rep) → replace all
│   ├─ repeat(n)            → repeat string
│   ├─ padStart(len, str)   → pad start
│   └─ padEnd(len, str)     → pad end
│
├── Search / Find
│   ├─ includes(substr)     → check existence
│   ├─ indexOf(substr)      → first index
│   ├─ lastIndexOf(substr)  → last index
│   ├─ search(regex)        → first match index
│   ├─ match(regex)         → match regex
│   ├─ matchAll(regex)      → all regex matches
│   └─ localeCompare(str)   → compare strings
│
├── Case Conversion
│   ├─ toLowerCase()        → lowercase
│   ├─ toUpperCase()        → uppercase
│   ├─ toLocaleLowerCase()  → lowercase locale-aware
│   └─ toLocaleUpperCase()  → uppercase locale-aware
│
├── Trim / Clean
│   ├─ trim()               → remove spaces both ends
│   ├─ trimStart() / trimLeft() → remove spaces start
│   └─ trimEnd() / trimRight()  → remove spaces end
│
├── Substring / Extract
│   ├─ slice(start,end)     → extract section
│   ├─ substring(start,end) → between indexes
│   └─ substr(start,len)    → from start for length
│
├── Conversion / Primitive
│   ├─ toString()           → string representation
│   ├─ valueOf()            → primitive string
│   ├─ raw()                → raw template literal
│   └─ toWellFormed()       → well-formed string
│
└── Unicode / Normalization
    └─ normalize()          → Unicode normalization

# <><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><>


# JavaScript `substring()` Method Examples

The `substring(start, end)` method returns the part of the string between the specified start and end indexes.

- `start` → starting index (inclusive)
- `end` → ending index (exclusive)

If:
- `start > end`, the values are **swapped**.
- `start` or `end` is **negative**, it's treated as `0`.

---

## 🧩 Example Code

```javascript
let text = "JavaScript";

// Extract from index 0 up to (but not including) index 4
let result1 = text.substring(0, 4); 
console.log(result1); 
// Output: "Java"

// Extract from index 4 to the end of the string
let result2 = text.substring(4); 
console.log(result2); 
// Output: "Script"

// startIndex is greater than endIndex, so they are effectively swapped
let result3 = text.substring(7, 2); 
console.log(result3); 
// Output: "vaScr"

// Using negative index (treated as 0)
let result4 = text.substring(-5, 3); 
console.log(result4); 
// Output: "Jav"
````

---

✅ **Key Notes**

* Negative values are treated as `0`.
* If `start > end`, JavaScript automatically swaps them.

```

# <><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><>


## 🖼️ Casing Methods in JavaScript

```
             +------------------------+
             |        String          |
             +------------------------+
                       |
         --------------------------------------
         |                                    |
 +--------------------+            +-------------------------+
 | toLowerCase()      |            | toUpperCase()           |
 | - Default casing   |            | - Default casing        |
 | - Locale ignored   |            | - Locale ignored        |
 +--------------------+            +-------------------------+
         |                                    |
         v                                    v
 +--------------------------+       +-------------------------------+
 | toLocaleLowerCase(locale)|       | toLocaleUpperCase(locale)     |
 | - Locale-specific rules  |       | - Locale-specific rules       |
 | - Handles special chars  |       | - Handles special chars       |
 +--------------------------+       +-------------------------------+
```

---

### ⚡ Quick Memory Tip

* 🅰️ `toLowerCase()` / `toUpperCase()` → **Simple**, always same result.
* 🌐 `toLocaleLowerCase()` / `toLocaleUpperCase()` → **Smart**, adjusts to **language rules**.


# <><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><>

